[package]
name = "fibonacci-dsp"
version = "1.0.1"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]

[dev-dependencies]
hound = "3.5"
serde_json = "1"
